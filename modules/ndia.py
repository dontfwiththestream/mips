import config

TYPE = 'MESSAGE'
HOOK = 'ndia'
HELP = config.H(HOOK, ": poo in the loo. If the prefix is anything other than !, the joke is lost.")

def Main(self, line, user):
    return "\x02DESIGNATED"
